import { useEffect, useState } from 'react';
import api from '../../api/api';
import { ActivityTable } from './AdminDashboard';
export default function ActivityLog(){const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[search,setSearch]=useState('');useEffect(()=>{api.get('/admin/activity').then(r=>setRows(Array.isArray(r.data)?r.data:[])).catch(e=>setError(e.response?.data?.message||'Could not load activity.')).finally(()=>setLoading(false))},[]);const list=rows.filter(r=>`${r.user?.name||''} ${r.action||''} ${r.description||''}`.toLowerCase().includes(search.toLowerCase()));return <div className="page-content"><section className="hero-banner"><div><span className="eyebrow">ADMIN · ACTIVITY</span><h1>Activity Log</h1><p>A complete audit trail for registrations, logins, logouts, and system actions.</p></div><div className="hero-leaf">◷</div></section>{error&&<div className="error-box">{error}</div>}<div className="card admin-table"><div className="table-toolbar"><div className="search-field"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search activity"/></div><span>{list.length} records</span></div>{loading?<div className="loading-card">Loading activity…</div>:<ActivityTable rows={list}/>}</div></div>}
