import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import api from '../api/api';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const {login}=useAuth();const navigate=useNavigate();const submit=async e=>{e.preventDefault();setError('');try{const r=await api.post('/login',{email,password});login(r.data.user,r.data.token);navigate('/dashboard')}catch(e){setError(e.response?.data?.message||'Invalid email or password.')}};return <div className="landing"><form className="landing-card" onSubmit={submit}><h1>Welcome back</h1><p>Sign in to MNC Resume Studio and continue your career journey.</p>{error&&<div className="error-box">{error}</div>}<label style={{display:'block',textAlign:'left',fontSize:11,fontWeight:700}}>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required placeholder="you@example.com" style={{display:'block',width:'100%',marginTop:6,padding:10,border:'1px solid #dce8e4',borderRadius:8}}/></label><label style={{display:'block',textAlign:'left',fontSize:11,fontWeight:700,marginTop:12}}>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required placeholder="••••••••" style={{display:'block',width:'100%',marginTop:6,padding:10,border:'1px solid #dce8e4',borderRadius:8}}/></label><button className="btn btn-primary" style={{width:'100%',marginTop:15}}>Login</button><p>Don't have an account? <Link to="/register" style={{color:'#159a68',fontWeight:800}}>Register</Link></p></form></div>}
