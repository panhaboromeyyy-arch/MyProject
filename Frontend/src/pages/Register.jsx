import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import api from '../api/api';
export default function Register(){const [form,setForm]=useState({name:'',email:'',password:'',password_confirmation:''});const [error,setError]=useState('');const {login}=useAuth();const navigate=useNavigate();const change=e=>setForm({...form,[e.target.name]:e.target.value});const submit=async e=>{e.preventDefault();setError('');try{const r=await api.post('/register',form);login(r.data.user,r.data.token);navigate('/dashboard')}catch(e){const m=e.response?.data?.errors;setError(m?Object.values(m).flat().join(' '):e.response?.data?.message||'Unable to create account.')}};return <div className="landing"><form className="landing-card" onSubmit={submit}><h1>Create your account</h1><p>Start building your professional profile and discovering opportunities.</p>{error&&<div className="error-box">{error}</div>}{[['name','Full name','text'],['email','Email','email'],['password','Password','password'],['password_confirmation','Confirm password','password']].map(([name,label,type])=><label key={name} style={{display:'block',textAlign:'left',fontSize:11,fontWeight:700,marginTop:10}}>{label}<input name={name} value={form[name]} onChange={change} type={type} required style={{display:'block',width:'100%',marginTop:6,padding:10,border:'1px solid #dce8e4',borderRadius:8}}/></label>)}<button className="btn btn-primary" style={{width:'100%',marginTop:15}}>Create account</button><p>Already have an account? <Link to="/login" style={{color:'#159a68',fontWeight:800}}>Login</Link></p></form></div>}
